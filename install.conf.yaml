- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
    ~/.bashrc: bash/bashrc
    ~/.bash_profile: bash/bash_profile
    ~/.inputrc: input/inputrc
    ~/.zshrc: zsh/zshrc
    ~/.gitattributes: git/gitattributes
    ~/.gitconfig: git/gitconfig
    ~/.gitignore: git/gitignore

- create:
    - ~/Downloads

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [git clean -f -d -f, Cleanup any dangling files]
  - [curl -fsSL https://starship.rs/install.sh | bash -s -- -b ~/.dotfiles/bin -y, Installing Starship prompt]
  # - [chsh -s $(which zsh), Set shell to zsh]
  # - [curl -fsSL https://starship.rs/install.sh | bash -s -- -b ~/.dotfiles/bin --platform unknown-linux-musl -y, Installing Starship prompt]
